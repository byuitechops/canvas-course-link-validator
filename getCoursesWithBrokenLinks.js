module.exports = function (coursesToValidate) {

    var coursesWithBrokenLinks = [];
    for ( /*all courses */ ) {

        //Run the post request to start the validator for each of the courses.

        //Poll using get request to see where we are at with each of the validations

        //Get the resulting object and stick it in the coursesWithBrokenLinks array

    }

    return coursesWithBrokenLinks;

}